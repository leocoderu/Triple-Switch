name: triple_switch
description: "Triple switch has three positions ON - WAIT - OFF"

version: 1.1.3

repository: https://github.com/leocoderu/triple_switch
homepage: https://leocode.ru

topics:
  - widgets

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1

screenshots:
  - description: Triple switch logo.
    path: screenshots/logo.png
