name: triple_switch
description: "Triple-position switch widget"

repository: https://github.com/leocoderu/triple_switch
homepage: https://github.com/leocoderu/triple_switch

version: 1.0.0

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1

flutter:
  uses-material-design: true
